package BirdClassification;

interface LiveNearWater {
  /**
   * Setting up water sources(salt or fresh) for waterfowl.
   */
  void source(String source);

  /**
   * Setting up living places(ocean, lake...etc) for waterfowl.
   */
  void places(String place);
}
